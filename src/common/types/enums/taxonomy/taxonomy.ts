export enum Usage {
  Users = 'users',
  Sessions = 'sessions',
  'Product Packages' = 'product_packages',
  Exhibitors = 'exhibitors',
}

export enum Visibility {
  'Admin' = 'admins',
  'User' = 'users',
}

export enum FilterInSearch {
  'Yes' = 'true',
  'No' = 'false',
}
