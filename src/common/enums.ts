export enum Role {
  USER = 'user',
}

export enum OtpPurpose {
  VERIFICATION = 'verification',
  FORGOT_PASSWORD = 'forgot-password',
}

export enum SafeMessageRole {
  USER = 'user',
  ASSISTANT = 'assistant',
}
